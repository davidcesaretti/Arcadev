/**
 * Stub para analytics (eventos de juego, page views, etc.)
 */

export function trackGameStart(_gameId: string): void {
  // TODO: analytics.track('game_start', { gameId })
}

export function trackGameOver(_gameId: string, _score: number): void {
  // TODO: analytics.track('game_over', { gameId, score })
}
